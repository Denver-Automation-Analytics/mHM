name: mhm-dev
channels:
  - conda-forge
dependencies:
  - python=3.13
  - compilers
  - pkg-config
  - cmake
  - make
  - ninja
  - netcdf-fortran
  - fypp
  - git
  - pip
  - numpy
  - scipy
  - pandas
  - pip:
    # Add pip-only packages below, for example:
    # - some-package
    # - another-package==1.2.3

